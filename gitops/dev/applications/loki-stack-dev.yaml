apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: loki-stack-dev
  namespace: argocd
spec:
  project: banking-dev-project
  
  source:
    repoURL: https://github.com/YonatanDas/Banking-Microservices-Platform.git
    targetRevision: main
    path: monitoring/loki
    helm:
      valueFiles:
        - values/dev.yaml
      # Removed loki.useTestSchema parameter - using explicit schemaConfig instead
  
  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring
  
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

